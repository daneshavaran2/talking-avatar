import { ConversationExperience } from '@/components/chat/ConversationExperience';

export const dynamic = 'force-dynamic';

/** صفحهٔ عمومی آواتار — نقطهٔ ورود کاربر نهایی. */
export default function HomePage() {
  return <ConversationExperience />;
}
